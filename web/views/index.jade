extends layout

block content
	h1= title
	p Welcome to #{title}

	div
		form(name="send-email", action="/", method="post")
			input(type="email", name="email")
			button(type="submit").
				send email!
